name: Docs

on:
  push:
    branches: [main]
    paths:
      - "src/**"
      - "README.md"
      - "pyproject.toml"
      - ".github/workflows/docs.yml"
  release:
    types: [published]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6

      - name: Setup Python 3.12
        uses: actions/setup-python@v6
        with:
          python-version: "3.12"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install --only-binary=:all: -e "."
          pip install --only-binary=:all: pdoc

      - name: Build API reference site with pdoc
        run: |
          set -euo pipefail
          mkdir -p docs-site
          python -m pdoc poli_page --output-directory docs-site

      - name: Verify pdoc produced an index
        run: |
          set -euo pipefail
          if [ ! -s docs-site/index.html ]; then
            echo "pdoc did not emit docs-site/index.html — refusing to deploy an empty site." >&2
            ls -la docs-site || true
            exit 1
          fi

      - name: Upload Pages artifact
        uses: actions/upload-pages-artifact@v5
        with:
          path: docs-site

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v5
